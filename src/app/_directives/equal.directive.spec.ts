import { EqualDirective } from './equal.directive';

describe('EqualDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualDirective();
    expect(directive).toBeTruthy();
  });
});
